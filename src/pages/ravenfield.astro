---
import Layout from "../layouts/Layout.astro";
import ScrollView from "../components/ScrollView.astro";
import { getCollection } from "astro:content";

const projects = (await getCollection("project"))
    .filter((e) => e.data.group == "Ravenfield")
    .sort((a, b) => {
        const da = a.data.date?.getTime() ?? 100;
        const db = b.data.date?.getTime() ?? 100;
        return db - da;
    });
---

<Layout title="Ravenfield">
    <ScrollView useMargin={true}>
        <div class="content">Jake was here</div>
    </ScrollView>
</Layout>

<style>
    .content {
        padding: 1rem;
    }
</style>

