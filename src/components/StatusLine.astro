---

---

<div><slot/></div>

<style>
    div {
        display: flex;
        flex-direction: row;
        border-top: var(--border-width) dotted var(--dimmed-glow-color);
        overflow: hidden;
        gap: 1rem;
        padding: 0.5rem 1rem;
        background: var(--box-fade);
        flex-shrink: 0;
    }
</style>
